# R.C.T.F.

 - **Role:** Архитектор/инженер производительности
 - **Context:** Практика 1: правила CASE.md (REL-1, API-1, OBS-1), метрики из problem.md; слабый артефакт — practices/practice_01/tests_load.md с размытыми порогами
 - **Task:** Переформулировать tests_load.md так, чтобы критерии pass/fail опирались только на подтверждённые источники (REL-1, API-1, OBS-1, problem.md). Убрать неподтверждённые нормы, сохранить разнообразие сценариев (Baseline/Step/Spike/Soak, длинный diff).
 - **Format:** Короткий markdown: таблица сценариев с критериями, методика в списках, явные ссылки на правила; без переписывания файла целиком, только минимальные правки.

## Полный запрос

Ты — инженер, работаешь по схеме R.C.T.F. Перепроверь и сожми practices/practice_01/tests_load.md:

Role: архитектор нагрузочного тестирования для AI‑помощника code review
Context: CASE.md (REL-1, API-1, OBS-1), problem.md (p95 ≤ 10с; 100% 413 при >20000), tests_integration.md (поведение на таймаут/413/логи)
Task: привести критерии pass/fail только к подтверждённым нормам; убрать неподтверждённые числа (ошибки ≤1%, стабилизация ≤60с, восстановление ≤2 мин, распределение задержек заглушки); сохранить сценарии и методику
Format: markdown-замены для строк таблицы и методики; 3–5 пунктов «что изменилось», 1–2 пункта «что не меняем»

## Что получили

Модель предложила:
- REL-1: «100% контролируемых ответов при таймауте LLM» вместо «≥99%»; оставить «p95 ≤ 10с» по problem.md.
- Удалить «ошибки ≤ 1%», «стабилизация ≤ 60с», «восстановление ≤ 2 мин» как неподтверждённые.
- API-1: «share_413 = 100%» оставить обязательным; «нет вызова LLM» указать как дополнительную проверку, если длина проверяется до LLM.
- Методика: убрать конкретные распределения заглушки; оставить требование симулировать задержки >10с.

## Что изменили в исходном артефакте

- Файл и раздел:
  - practices/practice_01/tests_load.md — таблица «Сценарии и профили», разделы «Методика» и «Критерии приёмки».
- Изменение:
  - Заменили «≥99% контролируемых ответов» на «100% при таймауте» (REL-1). Убрали «ошибки ≤ 1%», времена стабилизации/восстановления и распределения заглушки.
  - Уточнили строку «API-1 длинный diff»: «нет вызова LLM» — дополнительная проверка, не критерий.
- Как проверили:
  - CASE.md:65 (API-1), 66 (REL-1), 70 (OBS-1); problem.md:19–20 — p95 ≤ 10с и 100% 413; tests_integration.md:5–8 — ожидаемое поведение.
- Что отклонили:
  - Ввод новых SLO/ошибки %, любые внешние допущения; изменение rps/длительностей шагов (оставили как параметры тестплана).
