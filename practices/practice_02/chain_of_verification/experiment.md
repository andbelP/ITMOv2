# Chain of Verification

Файл ведёт OpenCode по вашим запросам. Агент записывает фактические результаты экспериментов и вносит изменения в связанные файлы. Свою оценку сообщайте ему в чате; вручную заполнять шаблон не нужно.

- Проверяемый черновик или утверждение:
- practices/practice_01/tests_load.md — соответствие правилам REL-1, API-1, OBS-1 и метрикам problem.md

## Запрос на проверку

Проверка соответствия tests_load.md правилам REL-1, API-1, OBS-1 и метрикам из problem.md

## Вопросы и результаты проверки

| Вопрос | Источник или проверка | Результат |
|---|---|---|
| REL-1/p95: Установлен ли целевой порог p95 ≤ 10с для времени ответа? | practices/practice_01/problem.md:19 — "p95 ≤ 10 c (REL-1)" | ОК |
| REL-1/таймауты: Требует ли REL-1 контролируемый ответ на каждый таймаут LLM (а не ≥99%)? | practices/practice_01/CASE.md:66 — "ошибка превращается в контролируемый ответ" | ОК |
| REL-1/error_rate: Есть ли в источниках порог "ошибки ≤ 1%"? | Неизвестно — в CASE.md/problem.md нет порога; tests_integration.md:6 — "Контролируемый ответ без 500" | Неизвестно |
| REL-1/Step стабилизация ≤60с: Есть ли требование времени стабилизации между шагами? | Источников нет | Неизвестно |
| REL-1/Spike восстановление ≤2 мин: Есть ли требование времени восстановления? | Источников нет | Неизвестно |
| REL-1/Soak тренд: Есть ли требование «отсутствие деградации тренда»? | Источников нет | Неизвестно |
| API-1/413: Должен ли сервис возвращать 413 при diff > 20000 и доля 413 = 100%? | practices/practice_01/CASE.md:65; problem.md:20; tests_integration.md:5 | ОК |
| API-1/нет вызова LLM: Гарантирует ли API-1 отсутствие вызова LLM при длинном diff? | CASE.md:65 (требует 413, порядок не оговорён) | Частично |
| OBS-1/состав логов: Что можно логировать и чего быть не должно? | practices/practice_01/CASE.md:70 — только request_id, длительность, статус; без содержимого diff | ОК |
| OBS-1/SECRET_MARKER: Должен ли маркер отсутствовать в логах? | practices/practice_01/tests_integration.md:8 — отсутствие содержимого diff и маркера | ОК |
| Профили нагрузок (rps/длительности): Подтверждены ли конкретные числа? | Источников нет — это параметры тестплана | Неизвестно |

## Исправленный результат

Фрагменты для замены в practices/practice_01/tests_load.md:

| Baseline | 10 rps, 2 мин warm-up + 5 мин стабильной нагрузки | p95 ≤ 10с; при симулированном таймауте LLM — 100% контролируемых ответов (REL-1) | p50/p90/p95 latency, error_rate, controlled_timeout_ratio | REL-1 |
| Step | 10 rps 3 мин → 30 rps 3 мин → 60 rps 3 мин → 90 rps 3 мин → 120 rps 3 мин | На каждом шаге p95 ≤ 10с; стабилизация метрик между шагами | p95, error_rate, backlog_trend (рост очереди) | REL-1 |
| Spike | 20 rps 3 мин → 200 rps 60с → 20 rps 3 мин | Во время спайка p95 ≤ 10с; восстановление p95 до базового уровня | p95, error_rate, recovery_time | REL-1 |
| Soak | 30 rps, 5 мин warm-up + 30 мин стабильной нагрузки | p95 ≤ 10с на всём интервале | p95 over time, error_rate, drift | REL-1 |
| API-1 длинный diff | 20 rps, 2 мин; diff ≈ 30000 символов | 100% запросов возвращают HTTP 413 (API-1); вызовы LLM не ожидаются, если проверка длины выполняется до вызова модели | share_413, trace_no_llm; проверка логов на отсутствие содержимого diff | API-1; OBS-1 |

- Для Step подтверждайте стабилизацию на каждом шаге (переход после стабилизации без роста очереди).
- Длинный diff: ≈ 30000 символов — для сценария API-1 (ожидаем HTTP 413; отсутствие обращения к LLM — если проверка длины выполняется до вызова модели).
- REL-1: во всех сценариях Baseline/Step/Spike/Soak p95 ≤ 10с. При симулированных таймаутах LLM — 100% контролируемых ответов (каждый таймаут → контролируемый ответ, REL-1).
- API-1: для входов с diff > 20000 символов доля ответов HTTP 413 = 100% (API-1); отсутствие вызовов LLM подтверждается, только если проверка длины выполняется до вызова модели.

## Что изменили в исходном артефакте

- Файл и раздел:
  - practices/practice_01/tests_load.md — таблица сценариев; разделы «Методика» и «Критерии приёмки».
- Изменение:
  - REL-1: заменили "доля контролируемых ответов ≥ 99%" на "100% контролируемых ответов при таймауте" с явной ссылкой на REL-1.
  - Убрали неподтверждённый порог "ошибки ≤ 1%" и жёсткие времена стабилизации/восстановления.
  - Уточнили API-1 длинный diff: 413 = 100% подтверждено; «без вызова LLM» помечено условно.
- Что отклонили:
  - Не меняли rps/длительности профилей нагрузки (параметры тестплана, не правила).
  - Оставили проверку SECRET_MARKER в логах как частный случай OBS-1.

Скрытые рассуждения модели не сохраняйте; нужны только вопросы, подтверждения и исправленный результат.
