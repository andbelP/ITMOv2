# RAG

Файл ведёт OpenCode по вашим запросам. Агент записывает фактические результаты экспериментов и вносит изменения в связанные файлы. Свою оценку сообщайте ему в чате; вручную заполнять шаблон не нужно.

- Вопрос к источникам:
Подтвердить критерии и методику из practices/practice_01/tests_load.md по REL-1, API-1, OBS-1; отметить, что не подтверждено напрямую, и предложить минимальные правки.

## Разрешённые источники

| Файл или документ | Зачем нужен | Какой фрагмент используем |
|---|---|---|
| practices/practice_01/CASE.md | Правила REL-1, API-1, OBS-1 | Строки с формулировками правил (REL-1, API-1, OBS-1) |
| practices/practice_01/problem.md | Метрика p95 и доля 413 | Таблица метрик (p95 ≤ 10c; 100% 413 при >20000) |
| practices/practice_01/tests_integration.md | Подтверждение ожидаемого поведения | Строки про HTTP 413, контролируемый ответ без 500, проверку логов |
| practices/practice_01/context.md | Контекст и ограничения | Разделы про Context Pack и ограничения |
| practices/practice_01/prompts.md | Связанные проверки в мастер-промпте | Разделы Master Prompt/Checks |
| practices/practice_01/tests_load.md | Проверяемый текст | Таблица сценариев, методика и критерии |

## Запрос


Ты — RAG-ассистент. Ответь строго на основе разрешённых источников. Верни ссылки file:lines и короткие цитаты.

Задача:
1) Подтверди утверждения и итог:
   - REL-1: p95 ≤ 10с; каждый таймаут LLM → контролируемый ответ; интеграция — контролируемый ответ без 500 при задержке >10с
   - API-1: HTTP 413 при diff > 20000; целевая доля отклонений 100% при >20000; отсутствие вызова LLM при длинном diff — проверить статус
   - OBS-1: логируются только request_id, длительность, статус; без diff; проверка SECRET_MARKER; упоминания в контексте/prompts
2) Укажи, что не подтверждено напрямую (Неизвестно) и предложи минимальные правки tests_load.md.
3) Выведи блок «Минимальные правки» как готовые заменяемые строки/абзацы для tests_load.md.

Формат ответа:
- Короткий итог (2–3 предложения)
- Таблица: | Утверждение | Файл:строки | Цитата | Вывод |
- Блок «Минимальные правки» (markdown)
- «Пропуски (Неизвестно)» — список с предложенной переформулировкой

## Ответ со ссылками на источники


Итог:
- Подтверждено: p95 ≤ 10с (problem.md), HTTP 413 при >20000 (CASE.md) и целевая доля 413 = 100% (problem.md), контролируемый ответ при таймауте LLM (CASE.md), состав логов без diff (CASE.md), проверка SECRET_MARKER (tests_integration.md).
- Не подтверждено напрямую: запрет/гарантия отсутствия вызова LLM при длинном diff; методические пороги стабилизации/восстановления; конкретное распределение задержек заглушки.

| Утверждение | Файл:строки | Цитата | Вывод |
|---|---|---|---|
| REL-1: p95 ≤ 10с | practices/practice_01/problem.md:19 | "p95 ≤ 10 c (REL-1)" | Подтверждено |
| REL-1: каждый таймаут → контролируемый ответ | practices/practice_01/CASE.md:66 | "ошибка превращается в контролируемый ответ" | Подтверждено |
| REL-1 интеграция: контролируемый ответ без 500 | practices/practice_01/tests_integration.md:6 | "Контролируемый ответ без 500" | Подтверждено |
| API-1: HTTP 413 при >20000 | practices/practice_01/CASE.md:65 | "diff длиннее 20 000 символов отклоняется с HTTP 413" | Подтверждено |
| API-1: 100% доля 413 | practices/practice_01/problem.md:20 | "100% при > 20000 символов (API-1)" | Подтверждено |
| API-1: отсутствие вызова LLM при длинном diff | — | — | Неизвестно |
| OBS-1: только request_id, длительность, статус | practices/practice_01/CASE.md:70 | "в лог пишутся только request_id, длительность и статус ..." | Подтверждено |
| OBS-1: SECRET_MARKER отсутствует в логах | practices/practice_01/tests_integration.md:8 | "В логах отсутствуют содержимое diff и маркер" | Подтверждено |

### Минимальные правки

Заменить строки в practices/practice_01/tests_load.md:

| Step | 10 rps 3 мин → 30 rps 3 мин → 60 rps 3 мин → 90 rps 3 мин → 120 rps 3 мин | На каждом шаге p95 ≤ 10с (REL-1); стабилизация — наблюдение (не критерий) | p95, error_rate, backlog_trend (рост очереди) | REL-1 |
| Spike | 20 rps 3 мин → 200 rps 60с → 20 rps 3 мин | Во время спайка p95 ≤ 10с (REL-1); восстановление — наблюдение (без порогов) | p95, error_rate, recovery_time | REL-1 |
| API-1 длинный diff | 20 rps, 2 мин; diff ≈ 30000 символов | 100% запросов возвращают HTTP 413 (API-1). Отсутствие вызовов LLM — дополнительная проверка, если длина проверяется до обращения к модели (не критерий) | share_413; при наличии трассировки — trace_no_llm; проверка логов на отсутствие содержимого diff | API-1; OBS-1 |

- LLM-заглушка: должна уметь симулировать задержки > 10с; при таймауте — контролируемый ответ (REL-1).
- API-1: для diff > 20000 доля 413 = 100% (API-1); проверка отсутствия вызова LLM — дополнительная, если проверка длины выполняется до обращения к модели (не критерий).

### Пропуски (Неизвестно)

- Нет прямого запрета/гарантии «нет вызова LLM при длинном diff» → оставить как дополнительную проверку, не как критерий.
- Нет норм про времена стабилизации/восстановления → оформить как наблюдение без порогов.
- Нет норм про распределения задержек заглушки → убрать числа; оставить требование симулировать >10с.
- Метрики backlog_trend/recovery_time/drift не определены → оставить как дополнительные измерения без порогов.

## Что изменили в исходном артефакте

- Файл и раздел:
  - practices/practice_01/tests_load.md — таблица сценариев; методика; критерии приёмки.
- Изменение:
  - Уточнили формулировки по REL-1 и API-1; исключили неподтверждённые пороги и числа.
- Как проверили ссылки:
  - Каждое утверждение снабжено file:lines + цитатой из разрешённых источников.
- Что отклонили как неподтверждённое:
  - «Нет вызова LLM при длинном diff» как обязательный критерий; конкретные времена стабилизации/восстановления; распределения заглушки.
